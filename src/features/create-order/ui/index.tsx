import { FC } from 'react';

import { ReceiptButton } from '@/shared/ui/receipt';

const CreateOrderButton: FC = () => {
  return (
    <ReceiptButton>Buy</ReceiptButton>
  );
};

export default CreateOrderButton;